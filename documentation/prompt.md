copiAdd to te[context.md](file;file:///Users/macos/production/documentation/context.md) 

Do not use tracking-wider!
And do not use emoji

Use lucide icon instead 

The UI is strecch show full UI 

For the filter please use admin with select dropdown (search and filter) 

And focus on the feature Bản đồ & Sự cố giao thông


Should show the bản đồ

The incident repot just a full screen dialog that show and the map show in a time range the incident and the flood map point, each map point on click will show the data source

And we need to make the system to scroing a final decision on which road is flooded and why weight so the map need a lot of distance in nearby Đại học Quốc tế for mock 


For the user, we need the bottom center glass like button and icon Home, Navigation, Redeemption and My profile

For Profile, please design setting, name, info, ... (like mobile app)

For the voucehr list of aviable voucer and curent :Điểm thưởng to exchange and exchange button uiwth real flow

For the navigation, this is core feature with search and navigation

For the navigation, i want to have the curretn location

Bearing and style for the future road is blocked by flood detected by the admin systemflow that decide based on data (historical, report incident, camera)
then they will get another route to get into the destination

After that we will build the open api wiki page /wiki with sidebar admin integeration api gudieline for the business to use GoSafe Open API data


Open API data also get from the camera in the area to detect so the map feature they will need the UI of image and rendered flood 
(You can use trained model on google) to put and the find a source video of a the road (When user toggle the camera  map) then  they click why the road is flooded due to the camera detected and its estiamte the distance 

This is a veryy large task 
And need to be prcesise UI like home page, nice frontend desing uisng shadcn and follow the style of modern and smart
Instead make it mock like emoji

For the incident all is real time detected so please make sure you take it carefullyl

Plan carefully task to task and put to documetnation 